import React, { Component } from 'react';
import {Image} from 'react-native';

export default class ImagemOK extends Component {

  render() {
    return (
      <Image source={require('../imgs/ok.png')} />
    );
  }  
}
